import LuntianLogo from '@/Components/LuntianLogo';

export default function ApplicationLogo({ className = '', compact = false }) {
    return <LuntianLogo className={className} compact={compact} />;
}
